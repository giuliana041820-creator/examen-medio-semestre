# Comandos Git

git init
git checkout -b feature/tasks
git add .
git commit -m "Implementacion de gestion de tareas y notificaciones"
git remote add origin <URL_DEL_REPOSITORIO>
git push origin feature/tasks

# Comandos Docker
docker-compose up -d
docker-compose down
